import React from "react";

import { StyledDiv } from "./styles";

const footer = () => {
  return (
    <StyledDiv>
      <h1> we are the footer</h1>
    </StyledDiv>
  );
};

export default footer;
